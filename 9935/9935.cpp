#include <iostream>
#include <stack>

using namespace std;
string str, str2;
stack <pair<char, int>> stk;
stack <char> stk2;

int main() {
    cin >> str;
    int len = str.length();

    cin >> str2;
    for(int i = 0; i < len; i++) {
        if(str[i] == )
    }
}
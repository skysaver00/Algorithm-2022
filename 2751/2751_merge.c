#include <stdio.h>

int arr[1000001];

void merge(int low, int mid, int high) {
    int i, j, k;
    i = low; j = mid + 1; k = high;

    while(i <= mid && j <= high) {
        if()
    }
}

int main() {

}
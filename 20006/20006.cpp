#include <iostream>
#include <vector>

using namespace std;
vector < pair<int, int> > player;
vector < pair<int, int> > room[301];

int main() {
    int p, m;
    cin >> p >> m;

    for(int i = 0; i < )

    return 0;
}